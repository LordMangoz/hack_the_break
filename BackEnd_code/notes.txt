get the file, on update of file.

scan for: 
    - Our set warning flags (element without a div, etc)
    - Forms without Labels
    - styling in-line, instead of with a css file. 
    - an id that isn't accessed.
    - Muliple css selecting same thing multiple times (could be unintentionally overwritting)
    - etc. 



